# ClinicFlow AI — i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add full Hebrew / English / Russian language support with a URL-based locale switcher (`/he/`, `/en/`, `/ru/`) across the entire ClinicFlow AI app.

**Architecture:** Use `next-intl` with App Router. All app pages move into `app/[locale]/`. Middleware handles locale routing + auth protection. Translation keys are extracted from all Hebrew strings into `messages/he.json`, `messages/en.json`, `messages/ru.json`. The `dir` attribute on `<html>` switches between `rtl` (Hebrew) and `ltr` (English, Russian) automatically.

**Tech Stack:** Next.js 14 App Router, next-intl v3, TypeScript, Tailwind CSS

---

## File Map

### New files
| File | Purpose |
|---|---|
| `messages/he.json` | Hebrew translations (source of truth) |
| `messages/en.json` | English translations |
| `messages/ru.json` | Russian translations |
| `i18n.ts` | next-intl config — request locale |
| `lib/i18n/navigation.ts` | Locale-aware Link, redirect, usePathname, useRouter |
| `components/layout/LanguageSwitcher.tsx` | עב / EN / RU switcher |

### Moved/restructured files
| Before | After |
|---|---|
| `app/layout.tsx` | becomes root redirect-only layout (no html tag) |
| `app/page.tsx` | `app/[locale]/page.tsx` |
| `app/(app)/layout.tsx` | `app/[locale]/(app)/layout.tsx` |
| `app/(app)/dashboard/page.tsx` | `app/[locale]/(app)/dashboard/page.tsx` |
| `app/(app)/leads/page.tsx` | `app/[locale]/(app)/leads/page.tsx` |
| `app/(app)/leads/[id]/page.tsx` | `app/[locale]/(app)/leads/[id]/page.tsx` |
| `app/(app)/clients/page.tsx` | `app/[locale]/(app)/clients/page.tsx` |
| `app/(app)/tasks/page.tsx` | `app/[locale]/(app)/tasks/page.tsx` |
| `app/(app)/reminders/page.tsx` | `app/[locale]/(app)/reminders/page.tsx` |
| `app/(app)/settings/page.tsx` | `app/[locale]/(app)/settings/page.tsx` |
| `app/(app)/documents/page.tsx` | `app/[locale]/(app)/documents/page.tsx` |
| `app/(app)/documents/[id]/page.tsx` | `app/[locale]/(app)/documents/[id]/page.tsx` |
| `app/(auth)/login/page.tsx` | `app/[locale]/(auth)/login/page.tsx` |
| `app/(auth)/register/page.tsx` | `app/[locale]/(auth)/register/page.tsx` |

### Modified files
| File | Change |
|---|---|
| `middleware.ts` | Combine next-intl locale routing + auth protection |
| `app/layout.tsx` | Becomes root layout without `<html>` — just redirect |
| `components/layout/Sidebar.tsx` | Use translations + locale-aware Links |
| `components/auth/LoginForm.tsx` | Use translations for labels/errors |
| `components/auth/RegisterForm.tsx` | Use translations |
| `components/reminders/CreateReminderForm.tsx` | Use translations |
| `components/settings/ProfileForm.tsx` | Use translations |
| `app/globals.css` | No change needed |

**API routes stay untouched** — they have no locale prefix.
**Admin pages stay untouched** — not in scope.

---

## Task 1: Install next-intl + Create Configuration

**Files:**
- Modify: `package.json` (install dependency)
- Create: `i18n.ts`
- Create: `lib/i18n/navigation.ts`

- [ ] **Step 1: Install next-intl**

```bash
cd "C:\Users\ירין שרייבהנד\Desktop\moveeasy israel"
npm install next-intl@^3
```

Expected: `added 1 package` (or similar), no errors.

- [ ] **Step 2: Create i18n.ts**

Create `i18n.ts` at the project root:

```typescript
import { getRequestConfig } from "next-intl/server"

export default getRequestConfig(async ({ locale }) => ({
  messages: (await import(`./messages/${locale}.json`)).default,
}))
```

- [ ] **Step 3: Create lib/i18n/navigation.ts**

```typescript
import { createSharedPathnamesNavigation } from "next-intl/navigation"

export const locales = ["he", "en", "ru"] as const
export type Locale = (typeof locales)[number]
export const defaultLocale: Locale = "he"

export const { Link, redirect, usePathname, useRouter } =
  createSharedPathnamesNavigation({ locales })
```

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json i18n.ts lib/i18n/navigation.ts
git commit -m "feat(i18n): install next-intl, add config + navigation helpers"
```

---

## Task 2: Create Translation Files

**Files:**
- Create: `messages/he.json`
- Create: `messages/en.json`
- Create: `messages/ru.json`

- [ ] **Step 1: Create messages/he.json**

```json
{
  "nav": {
    "login": "התחברות",
    "register": "התחל בחינם",
    "howItWorks": "איך זה עובד?"
  },
  "landing": {
    "badge": "בנוי לקליניקות אסתטיקה בישראל",
    "headline": "אף ליד לא נופל בין הכיסאות.",
    "subheadline": "ClinicFlow AI מרכז פניות, יוצר פולואפים אוטומטיים ועוזר לקליניקה לסגור יותר תורים בלי בלגן.",
    "startFree": "התחל בחינם",
    "statLeads": "% לידים שאובדים בלי follow-up",
    "statMinutes": "דקות להתחיל",
    "statPrice": "₪ לחודש כרגע",
    "statWorkflows": "תהליכי follow-up מובנים",
    "feature1Title": "ניהול לידים",
    "feature1Desc": "כל פנייה נרשמת, מסווגת ונכנסת לתהליך טיפול אוטומטי.",
    "feature2Title": "Follow-up אוטומטי",
    "feature2Desc": "המערכת יוצרת משימות follow-up עבור כל ליד ומזכירה לך בזמן הנכון.",
    "feature3Title": "סיכום AI",
    "feature3Desc": "AI מסכם כל פנייה ומדגיש את הנקודות החשובות לטיפול.",
    "feature4Title": "דשבורד בזמן אמת",
    "feature4Desc": "מבט מלא על כל הלידים, המשימות הפתוחות והתורים הקרובים.",
    "ctaTitle": "הקליניקה הבאה שלך. מאורגנת.",
    "ctaButton": "התחל בחינם",
    "footerText": "ClinicFlow AI — back office חכם לקליניקות אסתטיקה"
  },
  "auth": {
    "loginTitle": "התחברות",
    "loginSubtitle": "ברוך השב ל-ClinicFlow AI",
    "registerTitle": "יצירת חשבון",
    "registerSubtitle": "ניהול לידים חכם לקליניקה שלך",
    "email": "אימייל",
    "emailPlaceholder": "clinic@example.com",
    "password": "סיסמה",
    "passwordPlaceholder": "לפחות 8 תווים",
    "fullName": "שם מלא",
    "fullNamePlaceholder": "ד\"ר ישראל ישראלי",
    "submit": "התחבר",
    "registerSubmit": "יצירת חשבון",
    "alreadyHaveAccount": "יש לך חשבון?",
    "noAccount": "אין לך חשבון?",
    "loginLink": "התחבר",
    "registerLink": "הירשם",
    "loggingIn": "מתחבר..."
  },
  "sidebar": {
    "dashboard": "דשבורד",
    "leads": "לידים",
    "tasks": "משימות Follow-up",
    "clients": "לקוחות",
    "reminders": "תזכורות",
    "settings": "הגדרות",
    "logout": "התנתקות"
  },
  "dashboard": {
    "title": "דשבורד",
    "greeting": "שלום, {name}",
    "newLeadsToday": "לידים חדשים היום",
    "pendingTreatment": "ממתינים לטיפול",
    "overdueFollowups": "follow-ups באיחור",
    "appointmentsThisMonth": "תורים החודש",
    "recentLeads": "לידים אחרונים",
    "urgentFollowups": "follow-ups דחופים",
    "noLeads": "אין לידים עדיין",
    "addFirstLead": "הוסף ראשון",
    "noUrgentTasks": "אין משימות דחופות 🎉",
    "overdue": "⚠ איחור",
    "noDate": "ללא תאריך",
    "newLead": "+ ליד חדש",
    "phone": "טלפון",
    "treatment": "טיפול",
    "source": "מקור",
    "status": "סטטוס",
    "name": "שם"
  },
  "leads": {
    "title": "לידים",
    "newLead": "+ ליד חדש",
    "clientName": "שם לקוח",
    "phone": "טלפון",
    "treatment": "טיפול",
    "source": "מקור",
    "status": "סטטוס",
    "openTasks": "משימות פתוחות",
    "entered": "נכנס",
    "noLeads": "אין לידים עדיין",
    "breadcrumb": "לידים",
    "profileButton": "פרופיל לקוח",
    "bookButton": "+ קבע תור",
    "clientDetails": "פרטי לקוח",
    "fullName": "שם מלא",
    "treatmentWanted": "טיפול מבוקש",
    "notes": "הערות",
    "followupTasks": "משימות follow-up",
    "history": "היסטוריה",
    "leadEntered": "ליד נכנס",
    "aiSummarized": "AI סיכם את הפנייה ויצר משימות follow-up",
    "aiSummaryTitle": "✦ סיכום AI של הפנייה",
    "noTasks": "אין משימות"
  },
  "leadStatus": {
    "NEW": "חדש",
    "FOLLOW_UP": "follow-up",
    "QUOTED": "הצעת מחיר",
    "BOOKED": "נקבע תור",
    "WON": "נסגר",
    "LOST": "לא מתקדם"
  },
  "pipeline": {
    "NEW": "ליד חדש",
    "FOLLOW_UP": "follow-up",
    "QUOTED": "הצעת מחיר",
    "BOOKED": "נקבע תור",
    "WON": "נסגר"
  },
  "tasks": {
    "title": "משימות Follow-up",
    "noTasks": "אין משימות עדיין — צור ליד ראשון",
    "pendingOpen": "{count} פתוחות",
    "done": "{count} הושלמו",
    "empty": "אין משימות עדיין",
    "emptyDesc": "הוסף ליד ראשון — המערכת תיצור משימות follow-up אוטומטית",
    "addFirstLead": "+ ליד חדש",
    "backToLead": "← ליד",
    "task": "משימה",
    "client": "לקוח"
  },
  "taskStatus": {
    "PENDING": "ממתינות",
    "IN_PROGRESS": "בטיפול",
    "DONE": "הושלמו",
    "SKIPPED": "דולגו"
  },
  "taskStatusSingle": {
    "PENDING": "ממתין",
    "IN_PROGRESS": "בטיפול",
    "DONE": "הושלם",
    "SKIPPED": "דולג"
  },
  "reminders": {
    "title": "תזכורות",
    "eyebrow": "ניהול תזכורות",
    "subtitle": "{upcoming} עתידיות",
    "subtitleOne": "עתידית",
    "needsAttention": "{count} דורשות טיפול",
    "newReminder": "תזכורת חדשה",
    "remindMe": "תזכיר לי",
    "remindMePlaceholder": "לדוגמה: להתקשר למיכל לוי לאחר הטיפול",
    "when": "מתי?",
    "add": "הוספת תזכורת",
    "noReminders": "אין תזכורות עדיין",
    "noRemindersDesc": "צור תזכורת כדי לא לשכוח משימה חשובה.",
    "due": "הגיע הזמן",
    "upcoming": "עתידיות",
    "history": "היסטוריה",
    "markSeen": "סמן כנראה",
    "errorCreate": "שגיאה ביצירת התזכורת"
  },
  "clients": {
    "title": "לקוחות",
    "name": "שם",
    "phone": "טלפון",
    "treatment": "טיפול",
    "source": "מקור",
    "leads": "לידים",
    "joined": "הצטרף",
    "noClients": "אין לקוחות עדיין —",
    "addFirstLead": "הוסף ליד ראשון"
  },
  "settings": {
    "title": "הפרטים שלך",
    "eyebrow": "הפרופיל האישי",
    "subtitle": "הפרטים שלך לחשבון הקליניקה",
    "personalDetails": "פרטים אישיים",
    "detailsDesc": "הפרטים שלך — שם, אימייל וטלפון — מופיעים בפרופיל הקליניקה.",
    "fullName": "שם מלא",
    "fullNamePlaceholder": "ישראל ישראלי",
    "email": "אימייל",
    "emailHint": "לא ניתן לשינוי כרגע",
    "idNumber": "תעודת זהות",
    "idNumberPlaceholder": "9 ספרות",
    "idNumberHint": "אופציונלי",
    "phone": "טלפון",
    "phonePlaceholder": "050-0000000",
    "birthDate": "תאריך לידה",
    "save": "שמור שינויים",
    "saved": "✓ נשמר",
    "errorSave": "שגיאה בשמירה",
    "privacyTitle": "פרטיות",
    "privacyDesc": "המידע שלך נשמר מקומית במסד הנתונים שלנו ולא משותף עם אף גוף חיצוני. הסיסמה מוצפנת. המסמכים שלך נגישים רק לך."
  },
  "validation": {
    "nameMin": "השם חייב להכיל לפחות 2 תווים",
    "nameLong": "השם ארוך מדי",
    "emailInvalid": "כתובת אימייל לא תקינה",
    "emailLong": "האימייל ארוך מדי",
    "passwordMin": "הסיסמה חייבת להיות לפחות 8 תווים",
    "passwordLong": "הסיסמה ארוכה מדי",
    "passwordUppercase": "הסיסמה חייבת להכיל לפחות אות גדולה אחת",
    "passwordNumber": "הסיסמה חייבת להכיל לפחות ספרה אחת"
  },
  "common": {
    "noData": "—",
    "loading": "טוען...",
    "error": "שגיאה",
    "back": "חזרה",
    "metadata": "ClinicFlow AI"
  }
}
```

- [ ] **Step 2: Create messages/en.json**

```json
{
  "nav": {
    "login": "Log in",
    "register": "Start free",
    "howItWorks": "How it works?"
  },
  "landing": {
    "badge": "Built for aesthetic clinics in Israel",
    "headline": "No lead falls through the cracks.",
    "subheadline": "ClinicFlow AI centralizes inquiries, creates automatic follow-ups and helps your clinic close more appointments — without the chaos.",
    "startFree": "Start free",
    "statLeads": "% of leads lost without follow-up",
    "statMinutes": "minutes to start",
    "statPrice": "₪ / month right now",
    "statWorkflows": "built-in follow-up workflows",
    "feature1Title": "Lead management",
    "feature1Desc": "Every inquiry is recorded, categorized and enters an automatic treatment workflow.",
    "feature2Title": "Automatic follow-up",
    "feature2Desc": "The system creates follow-up tasks for every lead and reminds you at the right time.",
    "feature3Title": "AI summary",
    "feature3Desc": "AI summarizes every inquiry and highlights the important points for treatment.",
    "feature4Title": "Real-time dashboard",
    "feature4Desc": "Full view of all leads, open tasks and upcoming appointments.",
    "ctaTitle": "Your next clinic. Organized.",
    "ctaButton": "Start free",
    "footerText": "ClinicFlow AI — smart back office for aesthetic clinics"
  },
  "auth": {
    "loginTitle": "Log in",
    "loginSubtitle": "Welcome back to ClinicFlow AI",
    "registerTitle": "Create account",
    "registerSubtitle": "Smart lead management for your clinic",
    "email": "Email",
    "emailPlaceholder": "clinic@example.com",
    "password": "Password",
    "passwordPlaceholder": "At least 8 characters",
    "fullName": "Full name",
    "fullNamePlaceholder": "Dr. Jane Smith",
    "submit": "Log in",
    "registerSubmit": "Create account",
    "alreadyHaveAccount": "Already have an account?",
    "noAccount": "Don't have an account?",
    "loginLink": "Log in",
    "registerLink": "Sign up",
    "loggingIn": "Logging in..."
  },
  "sidebar": {
    "dashboard": "Dashboard",
    "leads": "Leads",
    "tasks": "Follow-up Tasks",
    "clients": "Clients",
    "reminders": "Reminders",
    "settings": "Settings",
    "logout": "Log out"
  },
  "dashboard": {
    "title": "Dashboard",
    "greeting": "Hello, {name}",
    "newLeadsToday": "New leads today",
    "pendingTreatment": "Pending treatment",
    "overdueFollowups": "Overdue follow-ups",
    "appointmentsThisMonth": "Appointments this month",
    "recentLeads": "Recent leads",
    "urgentFollowups": "Urgent follow-ups",
    "noLeads": "No leads yet",
    "addFirstLead": "Add first",
    "noUrgentTasks": "No urgent tasks 🎉",
    "overdue": "⚠ Overdue",
    "noDate": "No date",
    "newLead": "+ New lead",
    "phone": "Phone",
    "treatment": "Treatment",
    "source": "Source",
    "status": "Status",
    "name": "Name"
  },
  "leads": {
    "title": "Leads",
    "newLead": "+ New lead",
    "clientName": "Client name",
    "phone": "Phone",
    "treatment": "Treatment",
    "source": "Source",
    "status": "Status",
    "openTasks": "Open tasks",
    "entered": "Entered",
    "noLeads": "No leads yet",
    "breadcrumb": "Leads",
    "profileButton": "Client profile",
    "bookButton": "+ Book appointment",
    "clientDetails": "Client details",
    "fullName": "Full name",
    "treatmentWanted": "Desired treatment",
    "notes": "Notes",
    "followupTasks": "Follow-up tasks",
    "history": "History",
    "leadEntered": "Lead entered",
    "aiSummarized": "AI summarized the inquiry and created follow-up tasks",
    "aiSummaryTitle": "✦ AI summary of inquiry",
    "noTasks": "No tasks"
  },
  "leadStatus": {
    "NEW": "New",
    "FOLLOW_UP": "Follow-up",
    "QUOTED": "Quoted",
    "BOOKED": "Booked",
    "WON": "Won",
    "LOST": "Lost"
  },
  "pipeline": {
    "NEW": "New lead",
    "FOLLOW_UP": "Follow-up",
    "QUOTED": "Quote sent",
    "BOOKED": "Booked",
    "WON": "Closed"
  },
  "tasks": {
    "title": "Follow-up Tasks",
    "noTasks": "No tasks yet — create your first lead",
    "pendingOpen": "{count} open",
    "done": "{count} completed",
    "empty": "No tasks yet",
    "emptyDesc": "Add your first lead — the system will create follow-up tasks automatically",
    "addFirstLead": "+ New lead",
    "backToLead": "← Lead",
    "task": "Task",
    "client": "Client"
  },
  "taskStatus": {
    "PENDING": "Pending",
    "IN_PROGRESS": "In progress",
    "DONE": "Done",
    "SKIPPED": "Skipped"
  },
  "taskStatusSingle": {
    "PENDING": "Pending",
    "IN_PROGRESS": "In progress",
    "DONE": "Done",
    "SKIPPED": "Skipped"
  },
  "reminders": {
    "title": "Reminders",
    "eyebrow": "Manage reminders",
    "subtitle": "{upcoming} upcoming",
    "subtitleOne": "upcoming",
    "needsAttention": "{count} need attention",
    "newReminder": "New reminder",
    "remindMe": "Remind me",
    "remindMePlaceholder": "E.g.: Call after treatment",
    "when": "When?",
    "add": "Add reminder",
    "noReminders": "No reminders yet",
    "noRemindersDesc": "Create a reminder so you don't forget an important task.",
    "due": "Due now",
    "upcoming": "Upcoming",
    "history": "History",
    "markSeen": "Mark as seen",
    "errorCreate": "Error creating reminder"
  },
  "clients": {
    "title": "Clients",
    "name": "Name",
    "phone": "Phone",
    "treatment": "Treatment",
    "source": "Source",
    "leads": "Leads",
    "joined": "Joined",
    "noClients": "No clients yet —",
    "addFirstLead": "Add first lead"
  },
  "settings": {
    "title": "Your details",
    "eyebrow": "Personal profile",
    "subtitle": "Your details for the clinic account",
    "personalDetails": "Personal details",
    "detailsDesc": "Your details — name, email and phone — appear in the clinic profile.",
    "fullName": "Full name",
    "fullNamePlaceholder": "Jane Smith",
    "email": "Email",
    "emailHint": "Cannot be changed right now",
    "idNumber": "ID number",
    "idNumberPlaceholder": "9 digits",
    "idNumberHint": "Optional",
    "phone": "Phone",
    "phonePlaceholder": "050-0000000",
    "birthDate": "Date of birth",
    "save": "Save changes",
    "saved": "✓ Saved",
    "errorSave": "Error saving",
    "privacyTitle": "Privacy",
    "privacyDesc": "Your data is stored locally in our database and is not shared with any external party. Your password is encrypted. Your documents are accessible only to you."
  },
  "validation": {
    "nameMin": "Name must be at least 2 characters",
    "nameLong": "Name is too long",
    "emailInvalid": "Invalid email address",
    "emailLong": "Email is too long",
    "passwordMin": "Password must be at least 8 characters",
    "passwordLong": "Password is too long",
    "passwordUppercase": "Password must contain at least one uppercase letter",
    "passwordNumber": "Password must contain at least one number"
  },
  "common": {
    "noData": "—",
    "loading": "Loading...",
    "error": "Error",
    "back": "Back",
    "metadata": "ClinicFlow AI"
  }
}
```

- [ ] **Step 3: Create messages/ru.json**

```json
{
  "nav": {
    "login": "Войти",
    "register": "Начать бесплатно",
    "howItWorks": "Как это работает?"
  },
  "landing": {
    "badge": "Создано для эстетических клиник в Израиле",
    "headline": "Ни один лид не теряется.",
    "subheadline": "ClinicFlow AI централизует заявки, создаёт автоматические follow-up'ы и помогает клинике закрывать больше приёмов — без хаоса.",
    "startFree": "Начать бесплатно",
    "statLeads": "% лидов теряется без follow-up",
    "statMinutes": "минуты для старта",
    "statPrice": "₪ / месяц сейчас",
    "statWorkflows": "встроенных follow-up процессов",
    "feature1Title": "Управление лидами",
    "feature1Desc": "Каждая заявка записывается, классифицируется и попадает в автоматический процесс обработки.",
    "feature2Title": "Автоматический follow-up",
    "feature2Desc": "Система создаёт задачи follow-up для каждого лида и напоминает вам в нужное время.",
    "feature3Title": "Сводка AI",
    "feature3Desc": "AI суммирует каждую заявку и выделяет важные моменты для обработки.",
    "feature4Title": "Дашборд в реальном времени",
    "feature4Desc": "Полный обзор всех лидов, открытых задач и предстоящих приёмов.",
    "ctaTitle": "Ваша следующая клиника. Организована.",
    "ctaButton": "Начать бесплатно",
    "footerText": "ClinicFlow AI — умный бэк-офис для эстетических клиник"
  },
  "auth": {
    "loginTitle": "Войти",
    "loginSubtitle": "С возвращением в ClinicFlow AI",
    "registerTitle": "Создать аккаунт",
    "registerSubtitle": "Умное управление лидами для вашей клиники",
    "email": "Email",
    "emailPlaceholder": "clinic@example.com",
    "password": "Пароль",
    "passwordPlaceholder": "Не менее 8 символов",
    "fullName": "Полное имя",
    "fullNamePlaceholder": "Др. Иван Иванов",
    "submit": "Войти",
    "registerSubmit": "Создать аккаунт",
    "alreadyHaveAccount": "Уже есть аккаунт?",
    "noAccount": "Нет аккаунта?",
    "loginLink": "Войти",
    "registerLink": "Зарегистрироваться",
    "loggingIn": "Входим..."
  },
  "sidebar": {
    "dashboard": "Дашборд",
    "leads": "Лиды",
    "tasks": "Задачи Follow-up",
    "clients": "Клиенты",
    "reminders": "Напоминания",
    "settings": "Настройки",
    "logout": "Выйти"
  },
  "dashboard": {
    "title": "Дашборд",
    "greeting": "Привет, {name}",
    "newLeadsToday": "Новых лидов сегодня",
    "pendingTreatment": "Ожидают обработки",
    "overdueFollowups": "Follow-up с просрочкой",
    "appointmentsThisMonth": "Приёмов в этом месяце",
    "recentLeads": "Последние лиды",
    "urgentFollowups": "Срочные follow-up'ы",
    "noLeads": "Лидов пока нет",
    "addFirstLead": "Добавить первый",
    "noUrgentTasks": "Срочных задач нет 🎉",
    "overdue": "⚠ Просрочка",
    "noDate": "Без даты",
    "newLead": "+ Новый лид",
    "phone": "Телефон",
    "treatment": "Процедура",
    "source": "Источник",
    "status": "Статус",
    "name": "Имя"
  },
  "leads": {
    "title": "Лиды",
    "newLead": "+ Новый лид",
    "clientName": "Имя клиента",
    "phone": "Телефон",
    "treatment": "Процедура",
    "source": "Источник",
    "status": "Статус",
    "openTasks": "Открытые задачи",
    "entered": "Добавлен",
    "noLeads": "Лидов пока нет",
    "breadcrumb": "Лиды",
    "profileButton": "Профиль клиента",
    "bookButton": "+ Записать на приём",
    "clientDetails": "Данные клиента",
    "fullName": "Полное имя",
    "treatmentWanted": "Желаемая процедура",
    "notes": "Заметки",
    "followupTasks": "Задачи follow-up",
    "history": "История",
    "leadEntered": "Лид добавлен",
    "aiSummarized": "AI суммировал заявку и создал задачи follow-up",
    "aiSummaryTitle": "✦ Сводка AI заявки",
    "noTasks": "Задач нет"
  },
  "leadStatus": {
    "NEW": "Новый",
    "FOLLOW_UP": "Follow-up",
    "QUOTED": "Смета отправлена",
    "BOOKED": "Записан",
    "WON": "Закрыт",
    "LOST": "Не продвигается"
  },
  "pipeline": {
    "NEW": "Новый лид",
    "FOLLOW_UP": "Follow-up",
    "QUOTED": "Смета отправлена",
    "BOOKED": "Записан",
    "WON": "Закрыт"
  },
  "tasks": {
    "title": "Задачи Follow-up",
    "noTasks": "Задач пока нет — создайте первый лид",
    "pendingOpen": "{count} открытых",
    "done": "{count} выполнено",
    "empty": "Задач пока нет",
    "emptyDesc": "Добавьте первый лид — система автоматически создаст задачи follow-up",
    "addFirstLead": "+ Новый лид",
    "backToLead": "← Лид",
    "task": "Задача",
    "client": "Клиент"
  },
  "taskStatus": {
    "PENDING": "Ожидают",
    "IN_PROGRESS": "В работе",
    "DONE": "Выполнено",
    "SKIPPED": "Пропущено"
  },
  "taskStatusSingle": {
    "PENDING": "Ожидает",
    "IN_PROGRESS": "В работе",
    "DONE": "Выполнено",
    "SKIPPED": "Пропущено"
  },
  "reminders": {
    "title": "Напоминания",
    "eyebrow": "Управление напоминаниями",
    "subtitle": "{upcoming} предстоящих",
    "subtitleOne": "предстоящее",
    "needsAttention": "{count} требуют внимания",
    "newReminder": "Новое напоминание",
    "remindMe": "Напомнить мне",
    "remindMePlaceholder": "Например: Позвонить после процедуры",
    "when": "Когда?",
    "add": "Добавить напоминание",
    "noReminders": "Напоминаний пока нет",
    "noRemindersDesc": "Создайте напоминание, чтобы не забыть важное дело.",
    "due": "Время пришло",
    "upcoming": "Предстоящие",
    "history": "История",
    "markSeen": "Отметить как просмотренное",
    "errorCreate": "Ошибка при создании напоминания"
  },
  "clients": {
    "title": "Клиенты",
    "name": "Имя",
    "phone": "Телефон",
    "treatment": "Процедура",
    "source": "Источник",
    "leads": "Лиды",
    "joined": "Добавлен",
    "noClients": "Клиентов пока нет —",
    "addFirstLead": "Добавить первый лид"
  },
  "settings": {
    "title": "Ваши данные",
    "eyebrow": "Личный профиль",
    "subtitle": "Ваши данные для аккаунта клиники",
    "personalDetails": "Личные данные",
    "detailsDesc": "Ваши данные — имя, email и телефон — отображаются в профиле клиники.",
    "fullName": "Полное имя",
    "fullNamePlaceholder": "Иван Иванов",
    "email": "Email",
    "emailHint": "Изменить невозможно",
    "idNumber": "Номер удостоверения",
    "idNumberPlaceholder": "9 цифр",
    "idNumberHint": "Необязательно",
    "phone": "Телефон",
    "phonePlaceholder": "050-0000000",
    "birthDate": "Дата рождения",
    "save": "Сохранить изменения",
    "saved": "✓ Сохранено",
    "errorSave": "Ошибка при сохранении",
    "privacyTitle": "Конфиденциальность",
    "privacyDesc": "Ваши данные хранятся локально в нашей базе данных и не передаются третьим лицам. Пароль зашифрован. Ваши документы доступны только вам."
  },
  "validation": {
    "nameMin": "Имя должно содержать не менее 2 символов",
    "nameLong": "Имя слишком длинное",
    "emailInvalid": "Неверный email адрес",
    "emailLong": "Email слишком длинный",
    "passwordMin": "Пароль должен содержать не менее 8 символов",
    "passwordLong": "Пароль слишком длинный",
    "passwordUppercase": "Пароль должен содержать хотя бы одну заглавную букву",
    "passwordNumber": "Пароль должен содержать хотя бы одну цифру"
  },
  "common": {
    "noData": "—",
    "loading": "Загрузка...",
    "error": "Ошибка",
    "back": "Назад",
    "metadata": "ClinicFlow AI"
  }
}
```

- [ ] **Step 4: Commit**

```bash
git add messages/
git commit -m "feat(i18n): add translation files for Hebrew, English, Russian"
```

---

## Task 3: Update Middleware

**Files:**
- Modify: `middleware.ts`

- [ ] **Step 1: Replace middleware.ts with locale + auth aware version**

Replace the entire content of `middleware.ts`:

```typescript
import createMiddleware from "next-intl/middleware"
import { getToken } from "next-auth/jwt"
import { NextRequest, NextResponse } from "next/server"

const locales = ["he", "en", "ru"] as const
const defaultLocale = "he"

const protectedSegments = [
  "dashboard", "leads", "tasks", "clients",
  "reminders", "settings", "documents", "onboarding",
]

const intlMiddleware = createMiddleware({
  locales,
  defaultLocale,
  localePrefix: "always",
})

export default async function middleware(req: NextRequest) {
  const { pathname } = req.nextUrl

  // Let next-intl handle locale routing first
  const response = intlMiddleware(req)

  // Determine if this is a protected path: /{locale}/dashboard, /{locale}/leads, etc.
  const segments = pathname.split("/").filter(Boolean)
  const isProtected =
    segments.length >= 2 &&
    locales.includes(segments[0] as (typeof locales)[number]) &&
    protectedSegments.includes(segments[1])

  if (isProtected) {
    const token = await getToken({ req, secret: process.env.NEXTAUTH_SECRET })
    if (!token) {
      const locale = segments[0] ?? defaultLocale
      const loginUrl = new URL(`/${locale}/login`, req.url)
      loginUrl.searchParams.set("callbackUrl", pathname)
      return NextResponse.redirect(loginUrl)
    }
  }

  return response
}

export const config = {
  matcher: ["/((?!api|_next|_vercel|.*\\..*).*)"],
}
```

- [ ] **Step 2: Commit**

```bash
git add middleware.ts
git commit -m "feat(i18n): update middleware for locale routing + auth protection"
```

---

## Task 4: Restructure App Directory + Root Layout

**Files:**
- Modify: `app/layout.tsx` (root — redirect only, no html tag)
- Create: `app/[locale]/layout.tsx` (locale-aware html tag + NextIntlClientProvider)
- Modify: `app/[locale]/(app)/layout.tsx` (copy of current `app/(app)/layout.tsx`)

- [ ] **Step 1: Replace app/layout.tsx with root redirect layout**

Replace entire `app/layout.tsx`:

```typescript
import { redirect } from "next/navigation"

export default function RootLayout({ children }: { children: React.ReactNode }) {
  redirect("/he")
  return <>{children}</>
}
```

- [ ] **Step 2: Create app/[locale]/layout.tsx**

Create `app/[locale]/layout.tsx`:

```typescript
import type { Metadata } from "next"
import { Heebo, Frank_Ruhl_Libre, Rubik } from "next/font/google"
import "../globals.css"
import { Providers } from "@/components/layout/Providers"
import { NextIntlClientProvider } from "next-intl"
import { getMessages } from "next-intl/server"
import { notFound } from "next/navigation"

const locales = ["he", "en", "ru"] as const

const heebo = Heebo({
  subsets: ["hebrew", "latin"],
  variable: "--font-heebo",
  weight: ["300", "400", "500", "600", "700"],
  display: "swap",
})

const frankRuhlLibre = Frank_Ruhl_Libre({
  subsets: ["hebrew", "latin"],
  variable: "--font-frank",
  weight: ["400", "500", "700"],
  display: "swap",
})

// Rubik supports Hebrew, Latin, AND Cyrillic — used for Russian
const rubik = Rubik({
  subsets: ["hebrew", "latin", "cyrillic"],
  variable: "--font-rubik",
  weight: ["300", "400", "500", "600", "700"],
  display: "swap",
})

export const metadata: Metadata = {
  title: "ClinicFlow AI — Back Office לקליניקות אסתטיקה",
  description: "מרכז פניות, פולואפים אוטומטיים ודשבורד לידים לקליניקות אסתטיקה",
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode
  params: { locale: string }
}) {
  const { locale } = params

  if (!locales.includes(locale as (typeof locales)[number])) {
    notFound()
  }

  const messages = await getMessages()
  const dir = locale === "he" ? "rtl" : "ltr"
  // Use Rubik for Russian (Cyrillic support), Heebo for Hebrew/English
  const fontClass = locale === "ru"
    ? `${rubik.variable} ${frankRuhlLibre.variable}`
    : `${heebo.variable} ${frankRuhlLibre.variable}`

  return (
    <html lang={locale} dir={dir} className={fontClass}>
      <body className="bg-surface text-[var(--color-text)] font-sans antialiased">
        <NextIntlClientProvider messages={messages}>
          <Providers>{children}</Providers>
        </NextIntlClientProvider>
      </body>
    </html>
  )
}
```

- [ ] **Step 3: Create directory structure**

Run the following commands to create the new directory structure:

```bash
cd "C:\Users\ירין שרייבהנד\Desktop\moveeasy israel"

# Create locale directories
mkdir -p "app/[locale]/(app)/dashboard"
mkdir -p "app/[locale]/(app)/leads/[id]"
mkdir -p "app/[locale]/(app)/clients"
mkdir -p "app/[locale]/(app)/tasks/[taskId]"
mkdir -p "app/[locale]/(app)/reminders"
mkdir -p "app/[locale]/(app)/settings"
mkdir -p "app/[locale]/(app)/documents/[id]"
mkdir -p "app/[locale]/(auth)/login"
mkdir -p "app/[locale]/(auth)/register"
```

- [ ] **Step 4: Create app/[locale]/(app)/layout.tsx**

Create `app/[locale]/(app)/layout.tsx` (same structure as old `app/(app)/layout.tsx`):

```typescript
import { Sidebar } from "@/components/layout/Sidebar"
import { ReminderBell } from "@/components/layout/ReminderBell"

export default function AppLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex h-screen overflow-hidden bg-surface">
      <Sidebar />
      <main className="flex-1 flex flex-col overflow-hidden relative">
        <div className="absolute top-5 left-6 z-20">
          <ReminderBell />
        </div>
        {children}
      </main>
    </div>
  )
}
```

- [ ] **Step 5: Create app/[locale]/(auth)/layout.tsx**

```typescript
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
```

- [ ] **Step 6: Commit**

```bash
git add app/layout.tsx "app/[locale]/"
git commit -m "feat(i18n): restructure app directory for locale routing"
```

---

## Task 5: Update next.config.mjs for next-intl

**Files:**
- Modify: `next.config.mjs`

- [ ] **Step 1: Add next-intl plugin to next.config.mjs**

Replace `next.config.mjs`:

```javascript
import createNextIntlPlugin from "next-intl/plugin"

const withNextIntl = createNextIntlPlugin()

/** @type {import('next').NextConfig} */
const securityHeaders = [
  { key: "X-DNS-Prefetch-Control", value: "on" },
  { key: "Strict-Transport-Security", value: "max-age=63072000; includeSubDomains; preload" },
  { key: "X-Frame-Options", value: "SAMEORIGIN" },
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  { key: "Permissions-Policy", value: "camera=(), microphone=(), geolocation=(), browsing-topics=()" },
  {
    key: "Content-Security-Policy",
    value: [
      "default-src 'self'",
      "script-src 'self' 'unsafe-eval' 'unsafe-inline'",
      "style-src 'self' 'unsafe-inline' https://fonts.googleapis.com",
      "font-src 'self' https://fonts.gstatic.com",
      "img-src 'self' data: blob:",
      "connect-src 'self'",
      "frame-ancestors 'none'",
    ].join("; "),
  },
]

const nextConfig = {
  poweredByHeader: false,
  experimental: {
    serverComponentsExternalPackages: ["bcryptjs"],
  },
  images: {
    remotePatterns: [],
  },
  async headers() {
    return [{ source: "/(.*)", headers: securityHeaders }]
  },
}

export default withNextIntl(nextConfig)
```

- [ ] **Step 2: Commit**

```bash
git add next.config.mjs
git commit -m "feat(i18n): add next-intl plugin to next.config.mjs"
```

---

## Task 6: Update Sidebar + Create LanguageSwitcher

**Files:**
- Modify: `components/layout/Sidebar.tsx`
- Create: `components/layout/LanguageSwitcher.tsx`

- [ ] **Step 1: Create LanguageSwitcher.tsx**

Create `components/layout/LanguageSwitcher.tsx`:

```typescript
"use client"

import { useRouter, usePathname } from "@/lib/i18n/navigation"
import { useLocale } from "next-intl"

const LOCALES = [
  { code: "he", label: "עב" },
  { code: "en", label: "EN" },
  { code: "ru", label: "RU" },
] as const

export function LanguageSwitcher() {
  const locale = useLocale()
  const router = useRouter()
  const pathname = usePathname()

  const switchLocale = (newLocale: string) => {
    router.replace(pathname, { locale: newLocale })
  }

  return (
    <div className="flex items-center gap-1 px-3 py-2">
      {LOCALES.map(({ code, label }, i) => (
        <span key={code} className="flex items-center gap-1">
          <button
            onClick={() => switchLocale(code)}
            className={`text-xs font-semibold transition-colors ${
              locale === code
                ? "text-white"
                : "text-white/40 hover:text-white/70"
            }`}
          >
            {label}
          </button>
          {i < LOCALES.length - 1 && (
            <span className="text-white/20 text-xs">|</span>
          )}
        </span>
      ))}
    </div>
  )
}
```

- [ ] **Step 2: Update Sidebar.tsx**

Replace the entire `components/layout/Sidebar.tsx`:

```typescript
"use client"

import { usePathname } from "@/lib/i18n/navigation"
import { Link } from "@/lib/i18n/navigation"
import { useTranslations } from "next-intl"
import { cn } from "@/lib/utils"
import { LanguageSwitcher } from "./LanguageSwitcher"

export function Sidebar() {
  const t = useTranslations("sidebar")
  const pathname = usePathname()

  const navItems = [
    { href: "/dashboard", label: t("dashboard"), icon: "◉" },
    { href: "/leads",     label: t("leads"),     icon: "👥" },
    { href: "/tasks",     label: t("tasks"),      icon: "✓" },
    { href: "/clients",   label: t("clients"),    icon: "◫" },
    { href: "/reminders", label: t("reminders"),  icon: "◐" },
    { href: "/settings",  label: t("settings"),   icon: "◎" },
  ]

  return (
    <aside
      className="w-60 shrink-0 flex flex-col h-full relative overflow-hidden"
      style={{
        background: "linear-gradient(180deg, oklch(22% 0.07 245) 0%, oklch(18% 0.06 245) 100%)",
        color: "var(--color-primary-fg)",
      }}
    >
      <div
        aria-hidden
        className="absolute inset-0 opacity-[0.04] pointer-events-none"
        style={{
          backgroundImage: "radial-gradient(circle at 1px 1px, white 1px, transparent 0)",
          backgroundSize: "16px 16px",
        }}
      />

      <Link
        href="/dashboard"
        className="relative h-16 flex items-center px-5 border-b border-white/10"
      >
        <div className="flex items-baseline gap-1.5">
          <span className="font-display font-bold text-lg">ClinicFlow</span>
          <span className="text-[10px] text-[var(--color-highlight)] font-medium uppercase tracking-wider">
            AI
          </span>
        </div>
      </Link>

      <nav className="relative flex-1 p-3 flex flex-col gap-1 overflow-y-auto" aria-label="navigation">
        {navItems.map((item) => {
          const active = pathname === item.href || pathname.startsWith(item.href + "/")
          return (
            <Link
              key={item.href}
              href={item.href}
              className={cn(
                "relative flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm transition-colors duration-150",
                active
                  ? "bg-white/10 text-white font-semibold"
                  : "text-white/70 hover:bg-white/5 hover:text-white"
              )}
            >
              {active && (
                <span
                  className="absolute right-0 top-2 bottom-2 w-0.5 rounded-full"
                  style={{ background: "var(--color-highlight)" }}
                  aria-hidden
                />
              )}
              <span
                className={cn(
                  "font-mono text-base leading-none w-5 text-center",
                  active ? "text-[var(--color-highlight)]" : "opacity-70"
                )}
                aria-hidden
              >
                {item.icon}
              </span>
              <span>{item.label}</span>
            </Link>
          )
        })}
      </nav>

      <div className="relative border-t border-white/10">
        <LanguageSwitcher />
        <div className="px-3 pb-3">
          <Link
            href="/api/auth/signout"
            className="flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm text-white/70 hover:bg-white/5 hover:text-white transition-colors"
          >
            <span className="font-mono text-base leading-none w-5 text-center opacity-70" aria-hidden>←</span>
            <span>{t("logout")}</span>
          </Link>
        </div>
      </div>
    </aside>
  )
}
```

- [ ] **Step 3: Commit**

```bash
git add components/layout/Sidebar.tsx components/layout/LanguageSwitcher.tsx
git commit -m "feat(i18n): update Sidebar with translations + add LanguageSwitcher"
```

---

## Task 7: Migrate Landing Page + Auth Pages

**Files:**
- Create: `app/[locale]/page.tsx`
- Create: `app/[locale]/(auth)/login/page.tsx`
- Create: `app/[locale]/(auth)/register/page.tsx`

> **Note:** These are new files in the `[locale]` directory. The old files at `app/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/register/page.tsx` will remain temporarily until the old `(auth)` and root layouts are removed. After creating all locale pages, delete the old ones.

- [ ] **Step 1: Create app/[locale]/page.tsx**

Read `app/page.tsx` for its full content. Then create `app/[locale]/page.tsx` with translations:

```typescript
import Link from "next/link"
import { redirect } from "next/navigation"
import { getServerSession } from "next-auth"
import { authOptions } from "@/lib/auth/auth"
import { HeroMockup } from "@/components/landing/HeroMockup"
import { useTranslations } from "next-intl"
import { getTranslations } from "next-intl/server"
import { AnimatedCounter } from "@/components/landing/AnimatedCounter"
import { ScrollReveal } from "@/components/layout/ScrollReveal"

export async function generateMetadata({ params }: { params: { locale: string } }) {
  const t = await getTranslations({ locale: params.locale, namespace: "common" })
  return { title: `${t("metadata")} — Back Office לקליניקות אסתטיקה` }
}

export default async function HomePage({ params }: { params: { locale: string } }) {
  const session = await getServerSession(authOptions)
  if (session) redirect(`/${params.locale}/dashboard`)

  const t = await getTranslations({ locale: params.locale, namespace: "landing" })
  const tNav = await getTranslations({ locale: params.locale, namespace: "nav" })

  const stats = [
    { value: 40, label: t("statLeads") },
    { value: 3,  label: t("statMinutes") },
    { value: 0,  label: t("statPrice") },
    { value: 3,  label: t("statWorkflows") },
  ]

  const features = [
    { title: t("feature1Title"), desc: t("feature1Desc"), icon: "👥" },
    { title: t("feature2Title"), desc: t("feature2Desc"), icon: "✓" },
    { title: t("feature3Title"), desc: t("feature3Desc"), icon: "✦" },
    { title: t("feature4Title"), desc: t("feature4Desc"), icon: "◉" },
  ]

  return (
    <main className="min-h-screen bg-surface">
      <nav className="h-16 border-b border-border bg-surface-raised">
        <div className="max-w-6xl mx-auto h-full px-6 flex items-center justify-between">
          <Link href={`/${params.locale}`} className="font-display text-xl font-bold text-primary">
            ClinicFlow <span className="text-muted-foreground font-medium text-sm">AI</span>
          </Link>
          <div className="flex items-center gap-3">
            <Link href={`/${params.locale}/login`} className="text-sm text-muted-foreground hover:text-[var(--color-text)] px-3 py-2 transition-colors">
              {tNav("login")}
            </Link>
            <Link
              href={`/${params.locale}/register`}
              className="inline-flex items-center justify-center h-9 px-4 rounded-md bg-primary text-primary-foreground text-sm font-medium hover:bg-primary-hover transition-colors"
            >
              {tNav("register")}
            </Link>
          </div>
        </div>
      </nav>

      <section className="relative overflow-hidden">
        <div className="absolute inset-0 bg-gradient-to-b from-[var(--color-highlight-soft)] to-transparent pointer-events-none" aria-hidden />
        <div className="relative max-w-6xl mx-auto px-6 py-16 lg:py-24 grid lg:grid-cols-2 gap-12 lg:gap-8 items-center">
          <div className="animate-fade-in">
            <div className="inline-flex items-center gap-2 rounded-full bg-highlight-soft text-[var(--color-highlight)] px-3 py-1 text-xs font-semibold mb-5 border border-[var(--color-highlight)]/15">
              <span aria-hidden>✨</span>
              {t("badge")}
            </div>
            <h1 className="font-display text-4xl lg:text-5xl font-bold text-primary leading-tight mb-4">
              {t("headline")}
            </h1>
            <p className="text-lg text-muted-foreground leading-relaxed mb-8 max-w-lg">
              {t("subheadline")}
            </p>
            <div className="flex flex-wrap gap-3">
              <Link
                href={`/${params.locale}/register`}
                className="inline-flex items-center justify-center h-11 px-6 rounded-lg bg-primary text-primary-foreground text-sm font-semibold hover:bg-primary-hover transition-colors"
              >
                {t("startFree")}
              </Link>
              <button className="inline-flex items-center justify-center h-11 px-6 rounded-lg border border-border text-sm font-medium text-muted-foreground hover:bg-surface-raised transition-colors">
                {tNav("howItWorks")}
              </button>
            </div>
            <div className="flex flex-wrap gap-x-4 gap-y-1 mt-6">
              {["follow-up אוטומטי לכל ליד ✓", "סיכום AI לכל פנייה ✓", "דשבורד בזמן אמת ✓"].map((item) => (
                <span key={item} className="text-xs text-muted-foreground">{item}</span>
              ))}
            </div>
          </div>
          <div className="flex justify-center lg:justify-end">
            <HeroMockup />
          </div>
        </div>
      </section>

      <section
        className="py-8"
        style={{ background: "oklch(28% 0.04 40)" }}
      >
        <div className="max-w-6xl mx-auto px-6 grid grid-cols-2 md:grid-cols-4 gap-8">
          {stats.map((stat) => (
            <div key={stat.label} className="text-center">
              <AnimatedCounter value={stat.value} className="font-display text-4xl font-bold text-white tabular-nums" />
              <p className="text-xs text-white/60 mt-1 leading-snug">{stat.label}</p>
            </div>
          ))}
        </div>
      </section>

      <section className="py-20">
        <div className="max-w-6xl mx-auto px-6">
          <div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-5">
            {features.map((f) => (
              <ScrollReveal key={f.title}>
                <div className="bg-surface-raised rounded-2xl border border-border p-6 shadow-card h-full">
                  <span className="text-2xl mb-4 block" aria-hidden>{f.icon}</span>
                  <h3 className="font-display text-base font-bold text-primary mb-2">{f.title}</h3>
                  <p className="text-sm text-muted-foreground leading-relaxed">{f.desc}</p>
                </div>
              </ScrollReveal>
            ))}
          </div>
        </div>
      </section>

      <section className="py-20">
        <div className="max-w-2xl mx-auto px-6 text-center">
          <h2 className="font-display text-3xl font-bold text-primary mb-4">{t("ctaTitle")}</h2>
          <Link
            href={`/${params.locale}/register`}
            className="inline-flex items-center justify-center h-12 px-8 rounded-xl bg-primary text-primary-foreground text-base font-semibold hover:bg-primary-hover transition-colors"
          >
            {t("ctaButton")}
          </Link>
        </div>
      </section>

      <footer className="border-t border-border py-6">
        <div className="max-w-6xl mx-auto px-6 text-center text-xs text-muted-foreground">
          {t("footerText")}
        </div>
      </footer>
    </main>
  )
}
```

- [ ] **Step 2: Create app/[locale]/(auth)/login/page.tsx**

Read `app/(auth)/login/page.tsx` for the full JSX structure. Create `app/[locale]/(auth)/login/page.tsx`:

```typescript
import { Suspense } from "react"
import { redirect } from "next/navigation"
import { getServerSession } from "next-auth"
import { authOptions } from "@/lib/auth/auth"
import { LoginForm } from "@/components/auth/LoginForm"
import { getTranslations } from "next-intl/server"
import Link from "next/link"

export async function generateMetadata({ params }: { params: { locale: string } }) {
  const t = await getTranslations({ locale: params.locale, namespace: "auth" })
  return { title: `${t("loginTitle")} — ClinicFlow AI` }
}

export default async function LoginPage({ params }: { params: { locale: string } }) {
  const session = await getServerSession(authOptions)
  if (session) redirect(`/${params.locale}/dashboard`)

  const t = await getTranslations({ locale: params.locale, namespace: "auth" })

  return (
    <main className="min-h-screen flex items-center justify-center px-4 py-12 relative">
      <div
        aria-hidden
        className="absolute inset-0 pointer-events-none"
        style={{
          background:
            "radial-gradient(ellipse at 75% 20%, oklch(94% 0.05 10 / 0.6), transparent 60%), " +
            "radial-gradient(ellipse at 15% 90%, oklch(94% 0.04 10 / 0.4), transparent 60%)",
        }}
      />
      <div className="relative w-full max-w-sm flex flex-col gap-6">
        <div className="text-center">
          <Link href={`/${params.locale}`} className="font-display text-2xl font-bold text-primary">
            ClinicFlow <span className="text-muted-foreground font-medium text-sm">AI</span>
          </Link>
          <p className="text-sm text-muted-foreground mt-2">{t("loginSubtitle")}</p>
        </div>
        <div className="bg-surface-raised rounded-2xl border border-border p-6 shadow-card">
          <h1 className="font-display text-xl font-bold text-primary mb-5">{t("loginTitle")}</h1>
          <Suspense>
            <LoginForm locale={params.locale} />
          </Suspense>
        </div>
        <p className="text-center text-sm text-muted-foreground">
          {t("noAccount")}{" "}
          <Link href={`/${params.locale}/register`} className="text-[var(--color-highlight)] font-medium hover:underline">
            {t("registerLink")}
          </Link>
        </p>
      </div>
    </main>
  )
}
```

- [ ] **Step 3: Create app/[locale]/(auth)/register/page.tsx**

```typescript
import { redirect } from "next/navigation"
import { getServerSession } from "next-auth"
import { authOptions } from "@/lib/auth/auth"
import { RegisterForm } from "@/components/auth/RegisterForm"
import { getTranslations } from "next-intl/server"
import Link from "next/link"

export async function generateMetadata({ params }: { params: { locale: string } }) {
  const t = await getTranslations({ locale: params.locale, namespace: "auth" })
  return { title: `${t("registerTitle")} — ClinicFlow AI` }
}

export default async function RegisterPage({ params }: { params: { locale: string } }) {
  const session = await getServerSession(authOptions)
  if (session) redirect(`/${params.locale}/dashboard`)

  const t = await getTranslations({ locale: params.locale, namespace: "auth" })

  return (
    <main className="min-h-screen flex items-center justify-center px-4 py-12 relative">
      <div
        aria-hidden
        className="absolute inset-0 pointer-events-none"
        style={{
          background:
            "radial-gradient(ellipse at 75% 20%, oklch(94% 0.05 10 / 0.6), transparent 60%), " +
            "radial-gradient(ellipse at 15% 90%, oklch(94% 0.04 10 / 0.4), transparent 60%)",
        }}
      />
      <div className="relative w-full max-w-sm flex flex-col gap-6">
        <div className="text-center">
          <Link href={`/${params.locale}`} className="font-display text-2xl font-bold text-primary">
            ClinicFlow <span className="text-muted-foreground font-medium text-sm">AI</span>
          </Link>
          <p className="text-sm text-muted-foreground mt-2">{t("registerSubtitle")}</p>
        </div>
        <div className="bg-surface-raised rounded-2xl border border-border p-6 shadow-card">
          <h1 className="font-display text-xl font-bold text-primary mb-5">{t("registerTitle")}</h1>
          <RegisterForm locale={params.locale} />
        </div>
        <p className="text-center text-sm text-muted-foreground">
          {t("alreadyHaveAccount")}{" "}
          <Link href={`/${params.locale}/login`} className="text-[var(--color-highlight)] font-medium hover:underline">
            {t("loginLink")}
          </Link>
        </p>
      </div>
    </main>
  )
}
```

- [ ] **Step 4: Delete old pages that are now replaced**

```bash
# Old root page and auth pages are no longer needed
rm "app/page.tsx"
rm "app/(auth)/login/page.tsx"
rm "app/(auth)/register/page.tsx"
```

- [ ] **Step 5: Commit**

```bash
git add "app/[locale]/" app/page.tsx "app/(auth)/"
git commit -m "feat(i18n): migrate landing + auth pages to [locale] routing"
```

---

## Task 8: Migrate Dashboard Page

**Files:**
- Create: `app/[locale]/(app)/dashboard/page.tsx`

- [ ] **Step 1: Create the localized dashboard page**

Read `app/(app)/dashboard/page.tsx` for full content, then create `app/[locale]/(app)/dashboard/page.tsx`:

```typescript
import { redirect } from "next/navigation"
import { getServerSession } from "next-auth"
import { authOptions } from "@/lib/auth/auth"
import { prisma } from "@/lib/db/prisma"
import { Link } from "@/lib/i18n/navigation"
import { getTranslations } from "next-intl/server"

export async function generateMetadata({ params }: { params: { locale: string } }) {
  const t = await getTranslations({ locale: params.locale, namespace: "dashboard" })
  return { title: `${t("title")} — ClinicFlow AI` }
}

export default async function DashboardPage({ params }: { params: { locale: string } }) {
  const session = await getServerSession(authOptions)
  const userId = (session?.user as { id?: string } | undefined)?.id
  if (!userId) redirect(`/${params.locale}/login`)

  const t = await getTranslations({ locale: params.locale, namespace: "dashboard" })
  const tStatus = await getTranslations({ locale: params.locale, namespace: "leadStatus" })

  const STATUS_CLASSES: Record<string, string> = {
    NEW: "bg-blue-100 text-blue-700", FOLLOW_UP: "bg-amber-100 text-amber-800",
    QUOTED: "bg-purple-100 text-purple-800", BOOKED: "bg-green-100 text-green-800",
    WON: "bg-emerald-100 text-emerald-800", LOST: "bg-red-100 text-red-800",
  }

  const firstName = (session?.user?.name ?? "").split(" ")[0] || "משתמש"
  const today = new Date()
  const todayStart = new Date(today.getFullYear(), today.getMonth(), today.getDate())

  const [newLeadsToday, totalPending, overdueCount, bookedThisMonth, recentLeads, urgentTasks] =
    await Promise.all([
      prisma.lead.count({ where: { client: { userId }, createdAt: { gte: todayStart } } }),
      prisma.lead.count({ where: { client: { userId }, status: { notIn: ["WON", "LOST"] } } }),
      prisma.task.count({ where: { userId, leadId: { not: null }, status: { in: ["PENDING", "IN_PROGRESS"] }, dueDate: { lt: today } } }),
      prisma.lead.count({ where: { client: { userId }, status: "BOOKED", updatedAt: { gte: new Date(today.getFullYear(), today.getMonth(), 1) } } }),
      prisma.lead.findMany({
        where: { client: { userId } },
        include: { client: { select: { name: true, phone: true, source: true, treatmentWanted: true } } },
        orderBy: { createdAt: "desc" },
        take: 10,
      }),
      prisma.task.findMany({
        where: { userId, leadId: { not: null }, status: { in: ["PENDING", "IN_PROGRESS"] } },
        include: { workflowStep: { select: { title: true } }, lead: { include: { client: { select: { name: true } } } } },
        orderBy: { dueDate: "asc" },
        take: 5,
      }),
    ])

  const dateStr = today.toLocaleDateString(
    params.locale === "he" ? "he-IL" : params.locale === "ru" ? "ru-RU" : "en-US",
    { weekday: "long", day: "numeric", month: "long", year: "numeric" }
  )

  return (
    <div className="flex-1 overflow-auto bg-[var(--color-surface)] p-6 lg:p-8" dir={params.locale === "he" ? "rtl" : "ltr"}>
      <div className="flex items-center justify-between mb-6">
        <div>
          <h1 className="font-display text-2xl font-bold text-[var(--color-text)]">
            {t("greeting", { name: firstName })}
          </h1>
          <p className="text-sm text-[var(--color-muted-fg)] mt-1">{dateStr}</p>
        </div>
        <Link
          href="/leads/new"
          className="px-4 py-2 rounded-lg text-sm font-semibold text-white"
          style={{ background: "var(--color-highlight)" }}
        >
          {t("newLead")}
        </Link>
      </div>

      <div className="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-8">
        {[
          { label: t("newLeadsToday"), value: newLeadsToday, color: "var(--color-highlight)" },
          { label: t("pendingTreatment"), value: totalPending, color: "#d97706" },
          { label: t("overdueFollowups"), value: overdueCount, color: "#dc2626" },
          { label: t("appointmentsThisMonth"), value: bookedThisMonth, color: "var(--color-text)" },
        ].map(({ label, value, color }) => (
          <div key={label} className="bg-surface-raised rounded-2xl border border-border p-5 shadow-card">
            <p className="text-xs uppercase tracking-wide text-[var(--color-muted-fg)] mb-2">{label}</p>
            <p className="font-display text-4xl font-bold tabular-nums" style={{ color }}>{value}</p>
          </div>
        ))}
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <div className="lg:col-span-2">
          <h2 className="font-semibold text-sm text-[var(--color-muted-fg)] uppercase tracking-wide mb-3">{t("recentLeads")}</h2>
          <div className="bg-surface-raised rounded-2xl border border-border overflow-hidden shadow-card">
            <table className="w-full text-sm">
              <thead>
                <tr className="border-b border-border bg-[var(--color-surface)]">
                  {[t("name"), t("treatment"), t("source"), t("status")].map((h) => (
                    <th key={h} className="text-right text-xs text-[var(--color-muted-fg)] font-medium px-4 py-3">{h}</th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {recentLeads.map((lead) => (
                  <tr key={lead.id} className="border-b border-border/50 last:border-0 hover:bg-[var(--color-surface)] transition-colors">
                    <td className="px-4 py-3">
                      <Link href={`/leads/${lead.id}`} className="font-semibold text-[var(--color-text)] hover:underline">
                        {lead.client.name}
                      </Link>
                      <p className="text-xs text-[var(--color-muted-fg)]">{lead.client.phone}</p>
                    </td>
                    <td className="px-4 py-3 text-[var(--color-muted-fg)]">{lead.client.treatmentWanted ?? t("noData", undefined, { defaultValue: "—" })}</td>
                    <td className="px-4 py-3 text-[var(--color-muted-fg)]">{lead.client.source ?? "—"}</td>
                    <td className="px-4 py-3">
                      <span className={`inline-block px-2.5 py-0.5 rounded-full text-xs font-semibold ${STATUS_CLASSES[lead.status] ?? ""}`}>
                        {tStatus(lead.status as any)}
                      </span>
                    </td>
                  </tr>
                ))}
                {recentLeads.length === 0 && (
                  <tr>
                    <td colSpan={4} className="px-4 py-8 text-center text-[var(--color-muted-fg)] text-sm">
                      {t("noLeads")} — <Link href="/leads/new" className="underline">{t("addFirstLead")}</Link>
                    </td>
                  </tr>
                )}
              </tbody>
            </table>
          </div>
        </div>

        <div>
          <h2 className="font-semibold text-sm text-[var(--color-muted-fg)] uppercase tracking-wide mb-3">{t("urgentFollowups")}</h2>
          <div className="bg-surface-raised rounded-2xl border border-border shadow-card divide-y divide-border">
            {urgentTasks.map((task) => {
              const isOverdue = task.dueDate && task.dueDate < today
              return (
                <div key={task.id} className="flex items-center justify-between px-4 py-3">
                  <div>
                    <p className="font-semibold text-sm text-[var(--color-text)]">{task.lead?.client?.name ?? "—"}</p>
                    <p className="text-xs text-[var(--color-muted-fg)]">{task.workflowStep?.title ?? "—"}</p>
                  </div>
                  <span className={`text-xs font-semibold ${isOverdue ? "text-red-500" : "text-[var(--color-highlight)]"}`}>
                    {task.dueDate
                      ? isOverdue ? t("overdue") : task.dueDate.toLocaleDateString(params.locale === "he" ? "he-IL" : params.locale === "ru" ? "ru-RU" : "en-US", { day: "numeric", month: "short" })
                      : t("noDate")}
                  </span>
                </div>
              )
            })}
            {urgentTasks.length === 0 && (
              <div className="px-4 py-6 text-center text-sm text-[var(--color-muted-fg)]">
                {t("noUrgentTasks")}
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Delete old dashboard page**

```bash
rm "app/(app)/dashboard/page.tsx"
```

- [ ] **Step 3: Commit**

```bash
git add "app/[locale]/(app)/dashboard/" "app/(app)/dashboard/"
git commit -m "feat(i18n): migrate dashboard page"
```

---

## Task 9: Migrate Leads, Clients, Tasks, Reminders, Settings Pages

**Files:**
- Create: `app/[locale]/(app)/leads/page.tsx`
- Create: `app/[locale]/(app)/leads/[id]/page.tsx`
- Create: `app/[locale]/(app)/clients/page.tsx`
- Create: `app/[locale]/(app)/tasks/page.tsx`
- Create: `app/[locale]/(app)/reminders/page.tsx`
- Create: `app/[locale]/(app)/settings/page.tsx`
- Create: `app/[locale]/(app)/documents/page.tsx`
- Create: `app/[locale]/(app)/documents/[id]/page.tsx`

For each page below, read the existing page in `app/(app)/` to get the full JSX structure, then replicate it in `app/[locale]/(app)/` with these changes:
1. Add `{ params }: { params: { locale: string } }` to the component props
2. Replace `redirect("/login")` with `redirect(\`/${params.locale}/login\`)`
3. Import `getTranslations` from `"next-intl/server"` and `useTranslations` for client components
4. Replace all Hebrew string literals with `t("key")` calls using the translation keys defined in Task 2
5. Use `Link` from `"@/lib/i18n/navigation"` instead of `"next/link"` where locale-aware routing is needed
6. Add `generateMetadata` with translated title

- [ ] **Step 1: Create app/[locale]/(app)/leads/page.tsx**

Read `app/(app)/leads/page.tsx`. Create `app/[locale]/(app)/leads/page.tsx`:

Apply the transformation pattern above. The key string replacements are:
- `"לידים"` → `t("title")` (namespace: `"leads"`)
- `"+ ליד חדש"` → `t("newLead")`
- Table headers: `[t("clientName"), t("phone"), t("treatment"), t("source"), t("status"), t("openTasks"), t("entered")]`
- Status labels: `tStatus(lead.status)` using namespace `"leadStatus"`
- `"אין לידים עדיין"` → `t("noLeads")`
- `redirect("/login")` → `redirect(\`/${params.locale}/login\`)`

The `STATUS_CLASSES` map remains the same (only CSS classes, not translatable).

- [ ] **Step 2: Create app/[locale]/(app)/leads/[id]/page.tsx**

Read `app/(app)/leads/[id]/page.tsx`. Apply transformation:
- All Hebrew strings → translation keys from `"leads"` and `"pipeline"` namespaces
- `notFound()` stays as-is (from `"next/navigation"`)
- `redirect("/login")` → `redirect(\`/${params.locale}/login\`)`
- `PIPELINE` array labels → `tPipeline(step.status)` using namespace `"pipeline"`
- `STATUS_LABELS` for tasks → `tTaskStatus(task.status)` using namespace `"taskStatusSingle"`

- [ ] **Step 3: Create app/[locale]/(app)/clients/page.tsx**

Read `app/(app)/clients/page.tsx`. Apply transformation with namespace `"clients"`:
- Table headers: `[t("name"), t("phone"), t("treatment"), t("source"), t("leads"), t("joined")]`
- Empty state: `t("noClients")` + `t("addFirstLead")`

- [ ] **Step 4: Create app/[locale]/(app)/tasks/page.tsx**

Read `app/(app)/tasks/page.tsx`. Apply transformation with namespace `"tasks"` and `"taskStatus"`:
- Page title: `t("title")`
- Status labels: `tStatus(status)` from namespace `"taskStatus"`
- Subtitle: use `t("pendingOpen", { count: pending })` and `t("done", { count: done })`
- Empty state: `t("empty")`, `t("emptyDesc")`, `t("addFirstLead")`

- [ ] **Step 5: Create app/[locale]/(app)/reminders/page.tsx**

Read `app/(app)/reminders/page.tsx`. Apply transformation with namespace `"reminders"`:
- `PageHero` props: `eyebrow={t("eyebrow")}`, `title={t("title")}`
- Section labels: `t("due")`, `t("upcoming")`, `t("history")`
- Subtitle: `t("subtitle", { upcoming: upcoming.length })`
- If `dueUnseen.length > 0`: prepend `t("needsAttention", { count: dueUnseen.length })`

- [ ] **Step 6: Create app/[locale]/(app)/settings/page.tsx**

Read `app/(app)/settings/page.tsx`. Apply transformation with namespace `"settings"`:
- `PageHero`: `eyebrow={t("eyebrow")}`, `title={t("title")}`, `subtitle={t("subtitle")}`
- Section title: `t("personalDetails")`
- Description: `t("detailsDesc")`
- Privacy card: `t("privacyTitle")`, `t("privacyDesc")`

- [ ] **Step 7: Copy documents pages**

Read `app/(app)/documents/page.tsx` and `app/(app)/documents/[id]/page.tsx`. Create locale versions — apply the same redirect transformation but strings in documents pages can stay in Hebrew for now (documents is lower priority).

- [ ] **Step 8: Delete old (app) pages**

```bash
rm "app/(app)/leads/page.tsx"
rm "app/(app)/leads/[id]/page.tsx"
rm "app/(app)/clients/page.tsx"
rm "app/(app)/tasks/page.tsx"
rm "app/(app)/reminders/page.tsx"
rm "app/(app)/settings/page.tsx"
rm "app/(app)/documents/page.tsx"
rm "app/(app)/documents/[id]/page.tsx"
```

- [ ] **Step 9: Commit**

```bash
git add "app/[locale]/(app)/" "app/(app)/"
git commit -m "feat(i18n): migrate all app pages to locale routing"
```

---

## Task 10: Update Client-Side Form Components

**Files:**
- Modify: `components/auth/LoginForm.tsx`
- Modify: `components/auth/RegisterForm.tsx`
- Modify: `components/reminders/CreateReminderForm.tsx`
- Modify: `components/settings/ProfileForm.tsx`

Client components use `useTranslations` hook.

- [ ] **Step 1: Update LoginForm.tsx**

Read `components/auth/LoginForm.tsx`. Add locale prop and translations:

```typescript
"use client"
// Add to imports:
import { useTranslations } from "next-intl"

// Add locale prop to Props interface:
interface Props { locale?: string }

// Inside component, add:
const t = useTranslations("auth")

// Replace Hebrew labels/placeholders:
// label="אימייל" → label={t("email")}
// placeholder="clinic@example.com" → placeholder={t("emailPlaceholder")}
// label="סיסמה" → label={t("password")}
// Button text "התחבר" → {t("submit")}
// Loading text "מתחבר..." → {t("loggingIn")}
```

- [ ] **Step 2: Update RegisterForm.tsx**

Read `components/auth/RegisterForm.tsx`. Add `useTranslations("auth")` and replace Hebrew strings:
- `"שם מלא"` → `t("fullName")`
- `"ד\"ר ישראל ישראלי"` → `t("fullNamePlaceholder")`
- `"אימייל"` → `t("email")`
- `"סיסמה"` → `t("password")`
- Button → `t("registerSubmit")`

- [ ] **Step 3: Update CreateReminderForm.tsx**

Read `components/reminders/CreateReminderForm.tsx`. Add `useTranslations("reminders")`:
- `"תזכיר לי"` → `t("remindMe")`
- `placeholder` → `t("remindMePlaceholder")`
- `"מתי?"` → `t("when")`
- `"הוספת תזכורת"` → `t("add")`
- Error: `"שגיאה ביצירת התזכורת"` → `t("errorCreate")`

- [ ] **Step 4: Update ProfileForm.tsx**

Read `components/settings/ProfileForm.tsx`. Add `useTranslations("settings")`:
- `"שם מלא"` → `t("fullName")`
- `"ישראל ישראלי"` → `t("fullNamePlaceholder")`
- `"אימייל"` → `t("email")`
- `"לא ניתן לשינוי כרגע"` → `t("emailHint")`
- `"תעודת זהות"` → `t("idNumber")`
- `"9 ספרות"` → `t("idNumberPlaceholder")`
- `"אופציונלי"` → `t("idNumberHint")`
- `"טלפון"` → `t("phone")`
- `"050-0000000"` → `t("phonePlaceholder")`
- `"תאריך לידה"` → `t("birthDate")`
- `"שמור שינויים"` → `t("save")`
- `"✓ נשמר"` → `t("saved")`
- `"שגיאה בשמירה"` → `t("errorSave")`

- [ ] **Step 5: Commit**

```bash
git add components/auth/ components/reminders/CreateReminderForm.tsx components/settings/ProfileForm.tsx
git commit -m "feat(i18n): update client-side form components with translations"
```

---

## Task 11: Add Landing Page Language Switcher + Verify Build

**Files:**
- No new files — this task verifies and fixes build errors

- [ ] **Step 1: Run build to find TypeScript/import errors**

```bash
cd "C:\Users\ירין שרייבהנד\Desktop\moveeasy israel"
npm run build 2>&1 | head -60
```

Expected: Build succeeds. If there are errors, fix them before proceeding — they are almost certainly import path issues or missing `params` types.

Common fixes needed:
- Any `redirect("/login")` that wasn't updated → change to `redirect(\`/${params.locale}/login\`)`
- Any `import Link from "next/link"` in pages that navigate between app routes → change to `import { Link } from "@/lib/i18n/navigation"`
- Any `usePathname()` from `"next/navigation"` in client components → change to `usePathname()` from `"@/lib/i18n/navigation"`

- [ ] **Step 2: Run tests**

```bash
npm test
```

Expected: 13 passed. Tests don't use routing so they should not be affected.

- [ ] **Step 3: Start dev server and verify**

```bash
npm run dev
```

Open `http://localhost:3000` → should redirect to `http://localhost:3000/he`
Open `http://localhost:3000/en` → landing page in English
Open `http://localhost:3000/ru` → landing page in Russian
Open `http://localhost:3000/he/login` → login page in Hebrew (RTL)
Open `http://localhost:3000/en/login` → login page in English (LTR)

- [ ] **Step 4: Verify language switcher in sidebar**

Log in, go to `/he/dashboard`. Sidebar shows `עב | EN | RU` at the bottom. Clicking EN should navigate to `/en/dashboard` — same page, different language and direction.

- [ ] **Step 5: Final commit**

```bash
git add .
git commit -m "feat(i18n): full Hebrew/English/Russian support with locale routing"
```

---

## Self-Review

### Spec Coverage
- ✅ Hebrew (default, RTL) — Tasks 1-11
- ✅ English (LTR) — Tasks 2, 7-10
- ✅ Russian (LTR + Cyrillic font) — Tasks 2, 4, 7-10
- ✅ URL-based routing `/he/`, `/en/`, `/ru/` — Task 3
- ✅ Language switcher in sidebar — Task 6
- ✅ Language switcher in landing nav — Task 7
- ✅ Auth pages translated — Tasks 7, 10
- ✅ Dashboard translated — Task 8
- ✅ All app pages translated — Task 9
- ✅ Form components translated — Task 10
- ✅ Default locale = Hebrew — Tasks 1, 3

### Type Consistency
- `params.locale` typed as `string` throughout (Next.js App Router convention)
- `locales` array defined once in `lib/i18n/navigation.ts`, referenced by middleware
- `t("key")` calls use keys defined in Task 2 messages — consistent across all tasks
- `Link` from `@/lib/i18n/navigation` used for inter-app links; plain `next/link` for external/API links

### No Placeholders
All code blocks are complete. Translation keys match between message files and usage in components.
