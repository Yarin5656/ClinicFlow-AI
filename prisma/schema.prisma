// Prisma schema — MoveEasy Israel
// Full data model will be added in Phase 2. This is a Phase 1 stub
// that generates an empty PrismaClient so the app can compile.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
